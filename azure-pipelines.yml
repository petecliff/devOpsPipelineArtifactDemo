trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- bash: |
    echo "##vso[task.setvariable variable=runDownload;]Yes"

- task: UniversalPackages@0
  inputs:
    command: download
    vstsFeed: 'pim-ui-testing/qa-resources'
    vstsFeedPacakge:  'QA_Resources'
    downloadDirecotry: '$(Build.SourcesDirectory)/QA_Resources'
  condition: eq(variables['runDownload'], 'Yes')

- task: ExtractFiles@1
  inputs:
    archiveFilePatterns: '**/qa_resources.zip'
    destinationFolder: '$(Build.SourcesDirectory)/QA_Resources'

- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.x'
    addToPath: true

- task: PythonScript@0
  inputs:
    scriptSource: filePath
    scriptPath: "./build.py"

- task: UniversalPackages@0
  inputs:
    command: publish
    publishDirectory: '$(Build.SourcesDirectory)/QA_Resources'
    vstsFeedPublish: 'pim-poc/QA_Resources'
    vstsFeedPackagePublish: 'qa-resources'
    packagePublishDescription: 'Post-run QA resource files'



